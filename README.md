# goLangPractice-notes
package main

import (
	"fmt" //imported packages
	"math"
)

func main() {
	fmt.Println(math.Pi) //access math and access variable Pi, capitilization denoted if its public and private
}
